/*
 * config.js — small site-wide constants. Firebase values live in
 * firebase-config.js (the one file you edit after creating your project).
 */
window.APP_CONFIG = {
  // Bangladesh Standard Time: fixed +06:00, no daylight saving.
  TZ_OFFSET: '+06:00'
};
